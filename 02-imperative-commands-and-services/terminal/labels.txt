$ kubectl label redis tier=db
error: the server doesn't have a resource type "redis"
$ kubectl run redis --image=redis:alpine -l tier=db
pod/redis created
$ kubectl label pods redis tier=cache
error: 'tier' already has a value (db), and --overwrite is false
$ kubectl label pods redis tier=cache --overwrite
pod/redis labeled
$ kubectl label pods redis tier-
pod/redis unlabeled
$ kubectl get pods redis --show-labels
NAME    READY   STATUS    RESTARTS   AGE   LABELS
redis   1/1     Running   0          9s    <none>
