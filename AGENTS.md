# Agent notes

- Diagrams in this repo are generated with the skill in `.claude/skills/daniel-diagram/` (spec JSON → `scripts/render.py` → `scripts/capture.sh`). Keep the palette and fonts from that skill; commit the `.excalidraw` spec, the `.html` source and the PNG together.
- Each section folder (`01-pods`, …) has a README, `img/`, and the YAML that `--dry-run` generated. Screenshots come from a throwaway kind cluster.
- Commits are in Daniel's name only.
